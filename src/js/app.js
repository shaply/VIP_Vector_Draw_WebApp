import VectorDrawingApp from './VectorDraw/VectorDrawingApp.js';

document.addEventListener('DOMContentLoaded', function() {
    const app = new VectorDrawingApp();
    
    window.vectorApp = app;
});